# Source and candidate keyword demand — 2026-08-20

## Bing Keyword Research baseline

United States and global Bing data, 20 May–17 August 2026:

- `la times crossword`: 186K impressions; `la times crossword puzzle today`: 7.5K;
  `la times crossword answers`: 1.7K.
- `usa today crossword`: 139.8K impressions; `usa today crossword puzzle today`: 5.2K;
  `usa today crossword answers`: 1.2K.

These source families pass the base-demand gate. Exact newly published clue phrases
usually have too little history for Bing to show a trend, so source demand is combined
with a current entity/root trend rather than treated as exact-query volume.

## Google Trends near-term signal

United States Web Search, past seven days, with the most recent 2–3 days used for
direction. Values are relative indices, not absolute search volume.

- LA Times candidates at the latest observed hour versus seven-day average:
  `Tupac Shakur` 21 vs 12, `Dua Lipa` 22 vs 15, `Hades` 45 vs 33.
- USA TODAY candidates: `Arthur Ashe` 46 vs 12, `Eras Tour` 25 vs 18,
  `Bop It` 16 vs 11.

The six selected clue pages therefore meet both gates: a high-demand publication
family and an entity/root showing current demand above its recent average. Explanation
quality remains a publishing requirement, but it is not a selection reason.
